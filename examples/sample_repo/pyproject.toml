[project]
name = "campus-tasks"
version = "0.1.0"
description = "Campus Task Management — CodeForge demo repository"
requires-python = ">=3.9"
dependencies = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
